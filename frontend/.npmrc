---

### 🚀 Sube el cambio definitivo

Ahora vamos a mandar este archivo a GitHub para que Render no tenga opción de ignorarlo:

```bash
git add frontend/.npmrc
git commit -m "Fix: force legacy peer deps via .npmrc"
git push origin HEAD

